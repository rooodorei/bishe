# AI 提示词传递说明

本文档说明项目运行时实际会传给 AI 的提示词内容，按“剧情 AI”和“绘图 AI”分开整理。

## 1. 总体说明

项目中涉及两类 AI：

1. 剧情 AI：通过 OpenAI 兼容接口调用大语言模型，负责生成剧情文本、主角台词和安全审核。
2. 绘图 AI：通过 ComfyUI / Z-Image 工作流生成绘本图片。

当前正式绘图流程只调用 `workflows/zimage/story_page.json`，不再调用 `workflows/zimage/character_base.json`，因此不会生成角色定妆照。

## 2. 剧情 AI 会收到哪些提示词

剧情 AI 位于 `src/storybook_app/llm_engine.py`，一次剧情生成会拆成三个模型角色：

1. Director：剧情导演。
2. Actor：主角台词演员。
3. Critic：儿童内容审核员。

### 2.1 Director：剧情导演 AI

Director 是最核心的剧情生成 AI，负责生成：

- `narrator_text`：绘本旁白。
- `story_scene`：中文绘图场景提示词。
- `story_action`：中文绘图动作提示词。
- `options`：两个下一步选项。

#### 2.1.1 Director 的系统提示词

Director 会收到固定系统提示词，大意如下：

```text
你是一个专业的中文儿童互动绘本导演，同时熟悉 Z-Image/中文文生图模型的提示词写法。
请根据小朋友的选择构思下一幕，并严格输出一个标准 JSON 对象，不要输出 Markdown，不要添加解释。

JSON 必须包含以下字段：
{
    "narrator_text": "150字左右的中文生动旁白，适合儿童阅读，温暖、有画面感、有互动感",
    "story_scene": "中文绘图场景提示词",
    "story_action": "中文绘图动作提示词",
    "options": ["中文下一步选项1", "中文下一步选项2"]
}

绘图提示词要求：
1. story_scene 和 story_action 必须使用中文，不要再写英文提示词。
2. story_scene 描述环境、时间、氛围、光线、色彩、画面构图。
3. story_action 描述主角当前动作、姿态、表情和与环境的互动。
4. 不要在 story_scene 或 story_action 中重复主角外貌和服装；正式绘图流程会通过角色卡统一注入角色设定。
5. 每一幕只能围绕一个主角展开，不要生成双主角、群像或复杂危险画面。
6. 内容必须安全、温柔、童趣，避免暴力、血腥、恐怖、成人化、危险模仿。
7. 如果收到审核打回意见，请针对性修改，并保持 JSON 格式正确。
```

#### 2.1.2 Director 的用户消息

第一次创建故事时，Director 会收到：

```text
【主角特征】：用户前端输入或捏人生成的主角特征
【前情提要】：全局设定：故事主题是【用户选择的主题】。主角准备好冒险了。
【小朋友的选择】：开始冒险
```

例如：

```text
【主角特征】：小女孩，固定外貌：白色短发，圆脸，大眼睛，温柔微笑，固定服装：红色斗篷，浅黄色连衣裙，棕色小靴子，固定主色：红色，浅黄色，棕色，固定配饰：星星发卡，不携带额外道具，儿童绘本画风，温暖明亮，干净线条，柔和光线
【前情提要】：全局设定：故事主题是【魔法森林探险】。主角准备好冒险了。
【小朋友的选择】：开始冒险
```

后续点击选项或输入自定义选择时，Director 会收到：

```text
【主角特征】：用户最初输入或捏人生成的主角特征
【前情提要】：当前剧情树分支的剧情记忆
【小朋友的选择】：用户点击的选项或手动输入的选择
```

其中“前情提要”由 `database.rebuild_llm_context()` 从当前节点沿父节点回溯得到。格式大致如下：

```text
全局设定：故事主题是【魔法森林探险】。主角准备好冒险了。
故事开局：第一幕旁白
小朋友选择【某个选择】，剧情：对应剧情旁白
小朋友选择【某个选择】，剧情：对应剧情旁白
```

为了控制上下文长度，当前代码最终只保留：

- 全局设定。
- 最近 3 条剧情记忆。

如果上一轮被 Critic 审核打回，Director 的用户消息末尾还会追加：

```text
⚠️【上轮审核未通过，请修正】：审核员返回的 REJECT 原因
```

### 2.2 Actor：主角台词 AI

Actor 只负责生成一句主角第一人称台词，不负责剧情结构，也不负责绘图。

#### 2.2.1 Actor 的系统提示词

```text
你正在扮演儿童绘本主角。
请结合当前的【场景环境】和【旁白内容】，说一句中文第一人称台词。
要求：50字以内，童真、温暖、积极，像孩子会说的话；不要恐吓、攻击或成人化表达。
```

#### 2.2.2 Actor 的用户消息

```text
【当前场景】：Director 生成的 story_scene
【当前旁白】：Director 生成的 narrator_text
```

例如：

```text
【当前场景】：清晨的魔法森林小路，柔和阳光，发光小花，温暖明亮，儿童绘本插画
【当前旁白】：星星小红帽在清晨的花园里发现了一条会发光的小路，她决定轻轻挥手，向小路尽头走去。
```

Actor 返回的内容会作为 `actor_dialogue` 显示在前端。

### 2.3 Critic：儿童内容审核 AI

Critic 负责审核旁白和主角台词是否适合儿童绘本。

#### 2.3.1 Critic 的系统提示词

```text
你是儿童内容安全专家。
请审核旁白和台词是否适合儿童互动绘本。
重点检查：暴力、血腥、恐怖惊吓、成人暗示、危险模仿、歧视、羞辱、过度阴暗情绪。
如果安全，只回复 PASS。
如果不安全，回复 REJECT，并用中文简要说明需要修改的问题。
```

#### 2.3.2 Critic 的用户消息

```text
【旁白】：Director 生成的 narrator_text
【台词】：Actor 生成的 actor_dialogue
```

如果 Critic 返回 `PASS`，这一轮剧情通过。

如果 Critic 返回 `REJECT`，系统会把拒绝原因反馈给 Director，让 Director 重写剧情，最多重试 3 次。

## 3. 绘图 AI 会收到哪些提示词

绘图 AI 位于 `src/storybook_app/image_engine.py` 和 `src/storybook_app/character_card.py`。

当前正式流程只会把正向提示词注入到：

```text
workflows/zimage/story_page.json 的节点 4 的 inputs.text
```

不会再调用 `character_base.json`，也不会生成角色定妆照。

### 3.1 绘图正向提示词的来源

绘图提示词由三类信息组成：

1. 固定绘本画风和安全约束。
2. 角色卡提取出的固定角色设定。
3. Director 生成的 `story_action` 和 `story_scene`。

### 3.2 角色卡字段来源

前端会把捏人结果或用户手动输入作为 `child_features` 传给后端。

前端捏人生成的 `child_features` 大致格式是：

```text
小女孩，固定外貌：白色短发，圆脸，大眼睛，温柔微笑，固定服装：红色斗篷，浅黄色连衣裙，棕色小靴子，固定主色：红色，浅黄色，棕色，固定配饰：星星发卡，不携带额外道具，儿童绘本画风，温暖明亮，干净线条，柔和光线
```

后端 `character_card.py` 会从 `child_features` 中提取或生成以下字段：

| 字段 | 说明 |
| --- | --- |
| `role` | 主角身份，例如“可爱的小女孩”“可爱的小男孩”“拟人化的可爱小动物主角”。 |
| `hair` | 发型锚点，例如白色短发、黑色短发、棕色卷发、金色长发、双马尾等；会优先保留“白色短发”这类完整组合词。 |
| `face` | 固定默认值：圆脸，大眼睛，温柔微笑，儿童友好的表情。 |
| `clothes` | 固定服装锚点，例如斗篷、连衣裙、背带裤、鞋子、靴子等。 |
| `accessories` | 固定配饰锚点，例如发卡、书包、眼镜、围巾、帽子、徽章等。 |
| `colors` | 固定主色锚点，例如红色、蓝色、白色、棕色、金色、彩虹色等。 |
| `style` | 固定默认画风描述：儿童绘本画风，温暖明亮，干净线条，柔和光线，角色设计稿，高质量，可爱但不过度复杂。 |

注意：前端里的 `body`、`style` 等自由描述会包含在 `child_features` 中。当前绘图提示词会注入完整的前端角色卡原文 `card.raw_features`，并只追加一句简短一致性约束，避免同一套外貌、服装、配饰和主色重复出现多遍。后端提取出的结构化字段主要用于角色卡保存、调试和未来扩展。

### 3.3 `build_story_page_prompt()` 生成的正向提示词

`character_card.py` 会生成一段精简正向提示词。角色卡原文只注入一次，避免把同一套外貌、服装、配饰重复描述多遍：

```text
儿童绘本页面插画，温暖明亮，干净线条，柔和光线，儿童友好，
单个主角，不出现第二个主角，
角色卡：{card.raw_features}，
严格保持角色卡中的外貌、发型、发色、服装、鞋子、配饰和主色，不换装，不新增配饰，
动作：{story_action}，
场景：{story_scene}，
全身或中景构图，构图清晰
```

其中：

- `{card.raw_features}` 是前端捏人或用户手动输入生成的完整角色卡原文。
- `{story_action}` 来自 Director 生成的 `story_action`。
- `{story_scene}` 来自 Director 生成的 `story_scene`。

### 3.4 `image_engine.py` 是否追加强化约束

当前 `image_engine.py` 不再额外追加重复的角色一致性约束，只负责把 `build_story_page_prompt()` 返回的正向提示词写入 `story_page.json` 节点 `4`。

### 3.5 最终传给绘图 AI 的正向提示词结构

最终传给 ComfyUI / Z-Image 的提示词结构如下：

```text
儿童绘本页面插画，温暖明亮，干净线条，柔和光线，儿童友好，
单个主角，不出现第二个主角，
角色卡：{card.raw_features}，
严格保持角色卡中的外貌、发型、发色、服装、鞋子、配饰和主色，不换装，不新增配饰，
动作：{story_action}，
场景：{story_scene}，
全身或中景构图，构图清晰
```

### 3.6 负向提示词是否传给绘图 AI

当前正式代码没有把负向提示词传给 ComfyUI。

`character_card.py` 中会生成 `negative_prompt`，内容大致是：

```text
不同角色，多个人，多角色，双人，群像，换衣服，改变服装颜色，改变发色，额外复杂配饰，成人化，性感，恐怖，血腥，暴力，阴暗惊悚，多个主角，复杂背景，遮挡身体，裁切身体，低质量，变形，坏手，坏脸，文字，水印，logo
```

`build_story_page_prompt()` 又会追加：

```text
不同服装，服装变化，错误服装颜色，缺少标志性服装，额外帽子，文字，水印，logo
```

但是 `image_engine.py` 当前写法是：

```text
positive, _negative = build_story_page_prompt(card, story_action, story_scene)
```

也就是说 `_negative` 被生成了，但没有注入到 `story_page.json` 的任何节点里。

因此当前真正传给绘图 AI 的只有正向提示词。

## 4. 哪些内容不会传给绘图 AI

当前绘图 AI 不会收到：

- 角色定妆照提示词。
- `workflows/zimage/character_base.json`。
- 负向提示词 `_negative`。
- Actor 生成的主角台词。
- 完整剧情树。
- 所有历史节点文本。

绘图 AI 只使用当前节点相关的三类信息：

```text
child_features 解析出的角色卡
story_action
story_scene
```

## 5. 简要总结

### 剧情 AI 收到

Director 收到：

```text
系统规则：儿童绘本导演，输出 JSON，生成旁白、场景、动作、两个选项，内容安全，绘图提示词中文。
用户内容：主角特征 + 前情提要 + 小朋友选择。
```

Actor 收到：

```text
系统规则：扮演主角，说一句 50 字以内第一人称中文台词。
用户内容：当前场景 + 当前旁白。
```

Critic 收到：

```text
系统规则：儿童内容安全审核，只返回 PASS 或 REJECT 原因。
用户内容：旁白 + 主角台词。
```

### 绘图 AI 收到

绘图 AI 只收到 `story_page.json` 节点 4 的正向提示词：

```text
儿童绘本页面插画 + 一个主角约束 + 前端角色卡完整内容 + Director 生成的动作 + Director 生成的场景 + 简短一致性约束
```
